---
description: "frhscopex design-skill-os — vendor master: design. Design principles from ten canonical books, for when a decision needs a reason behind it; read-only. Runs exactly as the vendor designed it."
argument-hint: "[target] [notes]"
allowed-tools: Bash(bash ${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh:*), Bash(bash "${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh":*), Bash(find:*), Read
---
<!-- GENERATED by scripts/build.py from registry/tools.json — edit the registry (or the installed SKILL.md), then rebuild. Do not edit this file. -->

# frhscopex design-skill-os — vendor master: `design`

`/design-tools:design-skill-os` runs **design**, the skill this vendor calls its entry point, **the way the vendor
designed it** — its own discovery questions, modes, knobs, argument format and report. The plugin's
guardrails (read-only first, one tool per step) are deliberately *not* layered on top: this command
exists precisely to let the vendor's own flow run. Two things still hold, because they are yours,
not the plugin's: `BRAND-CONTRACT.md` answers the vendor's brand and preference questions
(its tokens and forbidden list are the answer, not a suggestion), and you end by saying what changed,
one line per file.

## Resolution

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh" design`

If the block above is empty or an error, find the resolver and run it yourself:
`bash "$(find ~/.claude/plugins "$HOME/Library/Mobile Documents/com~apple~CloudDocs/alex.levnikov.root/claude_plugins" -path '*design-tools/scripts/resolve.sh' 2>/dev/null | head -1)" design`

## Request — in the vendor's own format

Argument format: `[target] [notes]`

**$ARGUMENTS**

## Do

1. `status: MISSING` → stop and say where it looked; do not improvise the vendor from memory.
2. Load it: `load: skill` → the Skill tool by name with the request; `load: read` → Read the `skill:`
   file (and `base:` first if printed) and follow it as if invoked. Relative `references/` and
   `scripts/` resolve against `dir:` — for vendors whose scripts assume a project-relative path,
   call them by that absolute `dir:`.
3. Resolve `BRAND-CONTRACT.md` (working directory, then parents) and hand it in wherever the vendor
   asks about brand, tokens, fonts, colours or preferences. If `.ui-craft/brief.md` exists, its §6
   applies too.
4. Follow the vendor's flow to its own end — its questions, its passes, its report. Do not
   substitute the plugin's steps. One constraint survives: **no second orchestrator-class skill in
   this context**; if the vendor's flow wants one, say so and stop.
5. Report what changed, one line per file.

**Lookup.** Answer, don't act. Change no files.

## This vendor's tools in the set

- `/design-tools:design-skill-os` — Design principles from ten canonical books, for when a decision needs a reason behind it; read-only. (read-only)

Vendor card: `wiki/vendors/design-skill-os.md`.
