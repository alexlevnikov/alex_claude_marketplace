---
description: "Emil Kowalski · improve-animations — Read-only: audits all existing motion and writes a plan for other agents. Read-only."
argument-hint: "[target file, path or component] [notes]"
allowed-tools: Bash(bash ${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh:*), Bash(bash "${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh":*), Bash(find:*), Read
---
<!-- GENERATED by scripts/build.py from registry/tools.json — edit the registry (or the installed SKILL.md), then rebuild. Do not edit this file. -->

# `improve-animations` (Emil Kowalski) — direct engagement

`/design-tools:emil-improve-animations` — you asked for **improve-animations** by name, so `design-tools` is not routing — it is
loading. The router's discipline still holds: read-only before write, the brand contract outranks
the vendor, say what changed. Reference card: `wiki/tools/improve-animations.md` · vendor: `wiki/vendors/emil.md`.
Whole-vendor entry point: `/design-tools:emil`.

## Resolution

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh" improve-animations`

If the block above is empty or an error, the plugin root was not substituted. Find the resolver and
run it yourself with Bash:
`bash "$(find ~/.claude/plugins "$HOME/Library/Mobile Documents/com~apple~CloudDocs/alex.levnikov.root/claude_plugins" -path '*design-tools/scripts/resolve.sh' 2>/dev/null | head -1)" improve-animations`

## Request

**$ARGUMENTS**

**Judge.** Read-only by contract: report, score or verdict — change no files.

## Do

1. **MISSING?** If the resolution block says `status: MISSING`, stop. Say where it looked and point at `wiki/tools/improve-animations.md` → *Install*. Do not improvise the pass from memory — that is exactly the failure this plugin exists to prevent.
2. **Load the tool.** `load: skill` → invoke `improve-animations` with the Skill tool, passing the request. `load: read` → Read the `skill:` file and follow it exactly as if it had been invoked with the request. Its relative `references/` resolve against `dir:`.
3. **Run it** on the request. This tool is read-only by contract: deliver the report, the score, the answer or the verdict — and change no files.
4. If the report recommends a write pass, name the tool it maps to (`/design-tools:tools-list`) and stop; do not start it unasked.

**For:** audit all motion and produce a plan for other agents
**Not for:** one component
