---
description: "taste-skill by Leonxlnx · full-output-enforcement — Modifier: bans placeholders, ellipses and truncated output in whatever the next tool writes. Read-only."
argument-hint: "[target file, path or component] [notes]"
allowed-tools: Bash(bash ${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh:*), Bash(bash "${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh":*), Bash(find:*), Read
---
<!-- GENERATED by scripts/build.py from registry/tools.json — edit the registry (or the installed SKILL.md), then rebuild. Do not edit this file. -->

# `full-output-enforcement` (taste-skill by Leonxlnx) — direct engagement

`/design-tools:taste-full-output-enforcement` — you asked for **full-output-enforcement** by name, so `design-tools` is not routing — it is
loading. The router's discipline still holds: read-only before write, the brand contract outranks
the vendor, say what changed. Reference card: `wiki/tools/full-output-enforcement.md` · vendor: `wiki/vendors/taste.md`.
Whole-vendor entry point: `/design-tools:taste`.

## Resolution

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh" full-output-enforcement`

If the block above is empty or an error, the plugin root was not substituted. Find the resolver and
run it yourself with Bash:
`bash "$(find ~/.claude/plugins "$HOME/Library/Mobile Documents/com~apple~CloudDocs/alex.levnikov.root/claude_plugins" -path '*design-tools/scripts/resolve.sh' 2>/dev/null | head -1)" full-output-enforcement`

## Request

**$ARGUMENTS**

**A modifier.** It changes how another tool writes; it does nothing on its own. Load it, then load the tool it modifies, and keep both in force for the whole write.

## Do

1. **MISSING?** If the resolution block says `status: MISSING`, stop. Say where it looked and point at `wiki/tools/full-output-enforcement.md` → *Install*. Do not improvise the pass from memory — that is exactly the failure this plugin exists to prevent.
2. **Load the tool.** `load: skill` → invoke `full-output-enforcement` with the Skill tool, passing the request. `load: read` → Read the `skill:` file and follow it exactly as if it had been invoked with the request. Its relative `references/` resolve against `dir:`.
3. **Run it** on the request. This tool is read-only by contract: deliver the report, the score, the answer or the verdict — and change no files.
4. If the report recommends a write pass, name the tool it maps to (`/design-tools:tools-list`) and stop; do not start it unasked.

**For:** bans placeholders and truncated output — switch on whenever the chosen tool will write a long file
**Not for:** use on its own; it modifies another tool
