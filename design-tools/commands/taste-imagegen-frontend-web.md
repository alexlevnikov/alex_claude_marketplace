---
description: "taste-skill by Leonxlnx · imagegen-frontend-web — Generates one horizontal reference image per section of a web surface before any code. Writes code."
argument-hint: "[target file, path or component] [notes]"
allowed-tools: Bash(bash ${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh:*), Bash(bash "${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh":*), Bash(find:*), Read
---
<!-- GENERATED by scripts/build.py from registry/tools.json — edit the registry (or the installed SKILL.md), then rebuild. Do not edit this file. -->

# `imagegen-frontend-web` (taste-skill by Leonxlnx) — direct engagement

`/design-tools:taste-imagegen-frontend-web` — you asked for **imagegen-frontend-web** by name, so `design-tools` is not routing — it is
loading. The guardrails still hold: read-only before write, the brand contract outranks
the vendor, say what changed. Reference card: `wiki/tools/imagegen-frontend-web.md` · vendor: `wiki/vendors/taste.md`.
Whole-vendor entry point: `/design-tools:taste`.

## Resolution

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh" imagegen-frontend-web`

If the block above is empty or an error, the plugin root was not substituted. Find the resolver and
run it yourself with Bash:
`bash "$(find ~/.claude/plugins "$HOME/Library/Mobile Documents/com~apple~CloudDocs/alex.levnikov.root/claude_plugins" -path '*design-tools/scripts/resolve.sh' 2>/dev/null | head -1)" imagegen-frontend-web`

## Request

**$ARGUMENTS**

**Orchestrator-class.** It takes art-direction decisions and wants a context of its own. Do not load a second orchestrator in this session. If the real ask is a whole new surface, `design-pipeline` runs it with gates and isolation — offer that before starting.

## Do

1. **MISSING?** If the resolution block says `status: MISSING`, stop. Say where it looked and point at `wiki/tools/imagegen-frontend-web.md` → *Install*. Do not improvise the pass from memory — that is exactly the failure this plugin exists to prevent.
2. **Load the tool.** `load: skill` → invoke `imagegen-frontend-web` with the Skill tool, passing the request. `load: read` → Read the `skill:` file and follow it exactly as if it had been invoked with the request. Its relative `references/` resolve against `dir:`.
3. **Brand contract.** Resolve `BRAND-CONTRACT.md` — working directory, then each parent. Its tokens, forbidden list, anti-slop laws and commerce laws outrank the vendor's opinion. If `.ui-craft/brief.md` exists, its §6 learned constraints apply too.
4. **Read-only first** on code you have not seen this session: look before you write.
5. **Run it** on the request. Stay inside this one tool — a second write-tool is a stacking question (`references/stacking.md`); a third is `design-pipeline`.
6. **Report what changed**, one line per file.

**For:** one horizontal reference image per section of a web surface
**Not for:** sessions without an image tool
