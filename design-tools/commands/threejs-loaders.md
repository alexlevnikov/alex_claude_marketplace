---
description: "threejs-loaders · cloudai · W technique — GLTF/OBJ/texture loaders and asset pipelines"
argument-hint: "[target file, path or component] [notes]"
allowed-tools: Bash(bash ${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh:*), Bash(bash "${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh":*), Bash(find:*), Read
---
<!-- GENERATED by scripts/build.py from registry/tools.json — edit the registry (or the installed SKILL.md), then rebuild. Do not edit this file. -->

# `threejs-loaders` — direct engagement

You asked for **threejs-loaders** by name, so `design-tools` is not routing — it is loading. The router's
discipline still holds: read-only before write, the brand contract outranks the vendor, say what
changed. Reference card: `wiki/tools/threejs-loaders.md`.

## Resolution

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/resolve.sh" threejs-loaders`

If the block above is empty or an error, the plugin root was not substituted. Find the resolver and
run it yourself with Bash:
`bash "$(find ~/.claude/plugins "$HOME/Library/Mobile Documents/com~apple~CloudDocs/alex.levnikov.root/claude_plugins" -path '*design-tools/scripts/resolve.sh' 2>/dev/null | head -1)" threejs-loaders`

## Request

**$ARGUMENTS**

**Technique.** A framework- or effect-specific how-to. It assumes the motion or 3D decision is already made; if it is not, route the taste question first (`/design-tools:tool`).

## Do

1. **MISSING?** If the resolution block says `status: MISSING`, stop. Say where it looked and point at `wiki/tools/threejs-loaders.md` → *Install*. Do not improvise the pass from memory — that is exactly the failure this plugin exists to prevent.
2. **Load the tool.** `load: skill` → invoke `threejs-loaders` with the Skill tool, passing the request. `load: read` → Read the `skill:` file and follow it exactly as if it had been invoked with the request. Its relative `references/` resolve against `dir:`.
3. **Brand contract.** Resolve `BRAND-CONTRACT.md` — working directory, then each parent. Its tokens, forbidden list, anti-slop laws and commerce laws outrank the vendor's opinion. If `.ui-craft/brief.md` exists, its §6 learned constraints apply too.
4. **Read-only first** on code you have not seen this session: look before you write.
5. **Run it** on the request. Stay inside this one tool — a second write-tool is a stacking question (`references/stacking.md`); a third is `design-pipeline`.
6. **Report what changed**, one line per file.

**For:** GLTF/OBJ/texture loaders and asset pipelines
**Not for:** React
