<!-- GENERATED by scripts/build.py from registry/tools.json — edit the registry (or the installed SKILL.md), then rebuild. Do not edit this file. -->

# `animate`

Adds or adjusts animation inside the project's motion token system (durations, easings, intensity).

**Vendor:** [ui-craft by educlopez](../vendors/ui-craft.md) · https://github.com/educlopez/ui-craft
**Mode:** W · **Class:** pass · **Group:** feel · **Routable:** yes — discovery proposes it freely

**Base:** this is a `ui-craft` lens — the base loads first. → [ui-craft](ui-craft.md)

**For:** animation inside the project's motion tokens
**Not for:** taste questions — use emil

## Engage

- Direct: `/design-tools:ui-craft-animate <target>`
- Vendor entry point: `/design-tools:ui-craft` — runs the vendor's master `ui-craft`
- Discovery: `/design-tools:discover <your words>` can propose it — no fixed phrasing recorded yet in `phrases.md`.

## What the vendor says

> Motion design pass — adds purposeful animations or removes excessive ones, respecting MOTION_INTENSITY and the project's animation stack. Use when the user asks to add animation, "make it feel smoother", fix janky transitions, or trim motion that's distracting. Invoke when the user asks for animate on their UI, or mentions 'animate' alongside design / UI / frontend work.

| | |
|---|---|
| `name` | `animate` |
| version | — |
| license | not declared in SKILL.md — check the repo |
| argument-hint | — |
| flags | — |

## Files

- `SKILL.md` — 40 lines

## Where it lives

- Resolved from: `root:design-studio`
- Path: `/Users/alexlevnikov/Library/Mobile Documents/com~apple~CloudDocs/alex.levnikov.root/design-studio/.claude/skills/animate/SKILL.md`
- Install / refresh: `npx skills add educlopez/ui-craft`
- Any project: `bash design-studio/scripts/skillset.sh install <set> <project>` copies it into `<project>/.claude/skills/`, after which the Skill tool sees it by name.

**Collisions** (`references/collisions.md`): 3. Motion — five vendors
