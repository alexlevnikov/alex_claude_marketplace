<!-- GENERATED by scripts/build.py from registry/tools.json — edit the registry (or the installed SKILL.md), then rebuild. Do not edit this file. -->

# `craft`

Builds a surface from the spec with ui-craft recipes (dashboard, landing, auth).

**Vendor:** [ui-craft by educlopez](../vendors/ui-craft.md) · https://github.com/educlopez/ui-craft
**Mode:** W · **Class:** orchestrator · **Group:** direction · **Routable:** direct or whole-vendor only (`/design-tools:ui-craft-craft`)

**Base:** this is a `ui-craft` lens — the base loads first. → [ui-craft](ui-craft.md)

**For:** build a surface from the spec; recipes for dashboard, landing, auth
**Not for:** e-commerce — no recipe, it falls back to standard build mode

## Engage

- Direct: `/design-tools:ui-craft-craft <target>`
- Vendor entry point: `/design-tools:ui-craft` — runs the vendor's master `ui-craft`
- Discovery lists it only as a whole-vendor option, or when the request names it; the command above is the direct path.

## What the vendor says

> One-shot build pipeline for a complete surface from an outcome recipe — inputs (or defaults) → composition → theme → build order → acceptance bar. Use when the user asks for a whole surface ("build me a dashboard", "hazme un dashboard") and expects a shippable result, not component-level help. Invoke when the user asks for craft on their UI, or mentions 'craft' alongside design / UI / frontend work.

| | |
|---|---|
| `name` | `craft` |
| version | — |
| license | not declared in SKILL.md — check the repo |
| argument-hint | — |
| flags | — |

## Files

- `SKILL.md` — 75 lines

## Where it lives

- Resolved from: `root:design-studio`
- Path: `/Users/alexlevnikov/Library/Mobile Documents/com~apple~CloudDocs/alex.levnikov.root/design-studio/.claude/skills/craft/SKILL.md`
- Install / refresh: `npx skills add educlopez/ui-craft`
- Any project: `bash design-studio/scripts/skillset.sh install <set> <project>` copies it into `<project>/.claude/skills/`, after which the Skill tool sees it by name.


