<!-- GENERATED by scripts/build.py from registry/tools.json — edit the registry (or the installed SKILL.md), then rebuild. Do not edit this file. -->

# `extract`

Pulls repeated markup into reusable components and tokens.

**Vendor:** [ui-craft by educlopez](../vendors/ui-craft.md) · https://github.com/educlopez/ui-craft
**Mode:** W · **Class:** pass · **Group:** look · **Routable:** yes — discovery proposes it freely

**Base:** this is a `ui-craft` lens — the base loads first. → [ui-craft](ui-craft.md)

**For:** pulling repeated markup into components and tokens
**Not for:** first-time token setup — that is `tokens`

## Engage

- Direct: `/design-tools:ui-craft-extract <target>`
- Vendor entry point: `/design-tools:ui-craft` — runs the vendor's master `ui-craft`
- Discovery pre-rank: these phrasings (`references/phrases.md`) point here:
  - "this markup repeats everywhere" · «одно и то же копипастится»

## What the vendor says

> Refactoring pass — extracts repeated Tailwind class combos and markup into components, and lifts magic values into design tokens. Use when the codebase has obvious duplication, hardcoded hex values or pixel sizes, or when the user says "clean this up" / "extract components" / "tokenize styles". Invoke when the user asks for extract on their UI, or mentions 'extract' alongside design / UI / frontend work.

| | |
|---|---|
| `name` | `extract` |
| version | — |
| license | not declared in SKILL.md — check the repo |
| argument-hint | — |
| flags | — |

## Files

- `SKILL.md` — 45 lines

## Where it lives

- Resolved from: `root:design-studio`
- Path: `/Users/alexlevnikov/Library/Mobile Documents/com~apple~CloudDocs/alex.levnikov.root/design-studio/.claude/skills/extract/SKILL.md`
- Install / refresh: `npx skills add educlopez/ui-craft`
- Any project: `bash design-studio/scripts/skillset.sh install <set> <project>` copies it into `<project>/.claude/skills/`, after which the Skill tool sees it by name.


