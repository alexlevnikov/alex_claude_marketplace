<!-- GENERATED by scripts/build.py from registry/tools.json — edit the registry (or the installed SKILL.md), then rebuild. Do not edit this file. -->

# `redesign`

ui-craft's redesign lens: re-composes an existing surface against the brief and tokens.

**Vendor:** [ui-craft by educlopez](../vendors/ui-craft.md) · https://github.com/educlopez/ui-craft
**Mode:** W · **Class:** orchestrator · **Group:** direction · **Routable:** no — direct engagement only (`/design-tools:ui-craft-redesign`)

**Base:** this is a `ui-craft` lens — the base loads first. → [ui-craft](ui-craft.md)

**For:** ui-craft's redesign lens for an existing surface
**Not for:** the router — a redesign is pipeline work

## Engage

- Direct: `/design-tools:ui-craft-redesign <target>`
- Vendor entry point: `/design-tools:ui-craft` — runs the vendor's master `ui-craft`
- Not routed. The router escalates this class of request to `design-pipeline` or asks; the command above is the deliberate by-pass.

## What the vendor says

> Redesign an existing site or app without losing what already works — audits the current surface first, classifies what to preserve (brand, IA, SEO, content), picks a refresh/reskin/rebuild scope, then modernizes deliberately. Use when the user says "redesign", "modernize this site", "refresh the UI", "make this look current", or points at an existing page/URL they want improved rather than rebuilt from scratch. Invoke when the user asks for redesign on their UI, or mentions 'redesign' alongside design / UI / frontend work.

| | |
|---|---|
| `name` | `redesign` |
| version | — |
| license | not declared in SKILL.md — check the repo |
| argument-hint | — |
| flags | — |

## Files

- `SKILL.md` — 44 lines

## Where it lives

- Resolved from: `root:design-studio`
- Path: `/Users/alexlevnikov/Library/Mobile Documents/com~apple~CloudDocs/alex.levnikov.root/design-studio/.claude/skills/redesign/SKILL.md`
- Install / refresh: `npx skills add educlopez/ui-craft`
- Any project: `bash design-studio/scripts/skillset.sh install <set> <project>` copies it into `<project>/.claude/skills/`, after which the Skill tool sees it by name.


